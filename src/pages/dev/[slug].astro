---
// import Layout from "@/layouts/Layout.astro";
// import NavBar from "@/components/NavBar";
// import { Image } from "astro:assets";
// import { getCollection, render, type CollectionEntry } from "astro:content";
//
//
// export const prerender = true;
//
// export async function getStaticPaths() {
//   const projects = await getCollection("dev", ({ data }) => data.type === "project");
//
//   return projects.map((project) => ({
//     params: { slug: project.id },
//     props: { project },
//   }));
// }
//
// const { project } = Astro.props;
// const { Content } = await render(project);
---

<!--
<Layout>
  <NavBar client:only="react" />

  <main class="max-w-4xl mx-auto px-4 py-12 text-body">
    <div class="mb-6">
      <a
        href="/dev"
        class="inline-flex items-center gap-2 text-muted hover:text-paper transition"
      >
        <svg
          class="w-4 h-4"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M15 19l-7-7 7-7"></path>
        </svg>
        Back to Dev
      </a>
    </div>

    <header class="mb-10">
      <h1 class="text-4xl font-bold text-paper mb-2">{project.data.title}</h1>
      <p class="text-muted text-sm mb-2">
        {
          project.data.date.toLocaleDateString("en-US", {
            year: "numeric",
            month: "long",
            day: "numeric",
          })
        }
        {project.data.category && ` • ${project.data.category}`}
      </p>

      {
        project.data.tech && (
          <div class="flex flex-wrap gap-2 mt-4">
            {project.data.tech.map((tech: string) => (
              <span class="bg-surface-2 text-body text-sm px-3 py-1 rounded-full border border-line">
                {tech}
              </span>
            ))}
          </div>
        )
      }

      {
        project.data.url && (
          <div class="flex mt-auto pt-8">
            <a
              href={project.data.url}
              class="text-sm px-4 py-2 bg-paper text-ink rounded hover:opacity-90 transition"
            >
              See Project
            </a>
          </div>
        )
      }
    </header>

    {
      project.data.coverImage?.filter(Boolean).length && (
        <div class="mb-12 rounded-lg overflow-hidden border border-line">
          <div class="flex overflow-x-auto space-x-4 p-4 bg-ink-2 rounded-lg">
            {project.data.coverImage.filter(Boolean).map((image) => (
              <div class="flex-none w-64 h-64 md:w-96 md:h-96">
                <Image
                  src={image}
                  alt={project.data.title}
                  class="w-full h-full object-contain rounded-lg"
                  format="webp"
                  sizes="(max-width: 768px) 100vw, 768px"
                />
              </div>
            ))}
          </div>
        </div>
      )
    }

    <article class="prose prose-invert prose-neutral prose-lg max-w-none">
      <Content />
    </article>

    <footer class="mt-16 pt-8 border-t border-line">
      <a
        href="/dev"
        class="inline-flex items-center gap-2 text-muted hover:text-paper transition"
      >
        <svg
          class="w-4 h-4"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M15 19l-7-7 7-7"></path>
        </svg>
        View All Projects
      </a>
    </footer>
  </main>
</Layout>
-->
